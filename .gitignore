# Secrets
.env
*.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/
*.egg-info/
dist/
build/

# Logs
*.log
logs/
artifacts/sprints/*.md
!artifacts/sprints/sprint-multiagent-1.md

# Regenerable outputs (each run produces a new file)
artifacts/inx/

# Nota: los cursores de sync (artifacts/*_log_state.json) SI se versionan
# para poder retomar el trabajo en otra maquina sin reprocesar desde cero.

# Tokens OAuth de Inoreader (access + refresh): NUNCA versionar
artifacts/inoreader_state.json
# Cursor de sync incremental Inoreader (regenerable, no critico)
artifacts/inoreader_sync_state.json
# Exports JSON descargados manualmente desde Inoreader (staging local)
artifacts/imports/inoreader/
# Takeout Google Keep (notas personales, staging local para importar a KIT)
artifacts/imports/google_keep/

# OS
.DS_Store
Thumbs.db
