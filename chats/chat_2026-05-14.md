# Agent Chat — 2026-05-14

_Fuente de verdad compartida del día. Todos los agentes leen este hilo completo antes de responder. Append-only._

## Participantes

- **David** — director (rol fijo). Puede entrar desde cualquier interfaz en cualquier momento.
- **Cursor** — asistente operando desde el IDE Cursor (firma por defecto salvo petición explícita).
- **Copilot**, **Claude**, **Codex** — agentes intercambiables. Cualquiera puede asumir cualquier función cuando se le solicita.

## Funciones asignables (cualquier agente, a petición)

- orquestación, síntesis, integración en VS Code, visión de conjunto
- análisis profundo, razonamiento lógico, revisión crítica, teoría
- generación de código, refactoring, tests, implementación técnica

## Formato de mensajes

```
**David [@Destinatario]:** mensaje
**Agente:** mensaje | propuesta | voto | evaluación
```

## Modos de decisión

| Símbolo            | Modo        | Cuándo usarlo                                |
| ------------------ | ----------- | -------------------------------------------- |
| 🗳️ VOTO #N        | Votación    | Decisión binaria rápida, mayoría simple      |
| 🔍 EVALUACIÓN #N   | Evaluación  | Comparar alternativas desde cada especialidad |
| 🎯 ESPECIALIDAD    | Delegación  | Dominio inequívoco — solo David lo asigna    |
| 💡 CREATIVIDAD #N  | Brainstorm  | Sin restricciones, enfoque fresco            |

## Marcadores canónicos

- `MEMORIA:` hecho o acuerdo duradero
- `BLOQUEO:` impedimento concreto
- `SIGUIENTE:` siguiente acción u owner
- `✅ CERRADO #N:` decisión finalizada

## Estado actual

- Decisiones abiertas: ninguna
- Última acción: inicio de sesión 2026-05-14

---

**Codex:** Inicio de sesion ejecutado con `python tools/session_protocol.py inicia`; contexto Git, memoria, chat y devlog revisados.

**Codex:** Sesion iniciada. Rama `main`, ultimo commit `50fbc79 Close session 2026-05-10`, sin cambios de codigo pendientes; solo esta creado el chat de hoy. No hay decisiones abiertas en `chats/chat_2026-05-14.md`. SIGUIENTE: @David indica el frente a activar; candidatos naturales por devlog reciente: backfill completo de `Tipo MAR` en INX o continuar validacion ABGD/Obsidian tras la rotacion local.

**Codex:** Implementada integracion Raindrop.io -> KIT como fuente externa: `agents/raindrop_agent.py`, `tools/raindrop_tools.py`, `tools/sync_raindrop_to_kit.py`, schema en `ensure_kit_external_fields.py`, docs y caso 16. MEMORIA: Raindrop.io alimenta KIT con bookmarks tageados `kit-import`/`RAINDROP_SEARCH_KIT`; no crea filas `raindrop:*` propias en INX, se materializa como `kit:<page_id>` via `sync_inx_links --source kit`.

**Codex:** MEMORIA: Raindrop.io alimenta KIT con bookmarks tageados `kit-import` o filtrados por `RAINDROP_SEARCH_KIT`; no crea filas `raindrop:*` propias en INX, se materializa como `kit:<page_id>` via `sync_inx_links --source kit`.

**Codex:** MEMORIA: Los upserts Inoreader/Raindrop deduplican KIT por `Enlace` normalizado antes que por ID de fuente; la normalizacion elimina tracking (`utm_*`, `fbclid`, etc.), fragmentos y variantes triviales. Para historico usar `python tools/dedupe_notion_db.py --db-env NOTION_DB_KIT --key Enlace --normalize-url`.

**Codex:** Cierre de sesion: integracion Raindrop.io -> KIT implementada, OAuth validado, `kit-import` detectado con 1 bookmark importable en dry-run, dedupe por URL normalizada aplicado a Inoreader/Raindrop. Validaciones: `python -m pytest -q` = 27 passed; `python tools/memory_check.py` = OK. SIGUIENTE: @David cuando quieras importar el bookmark real, ejecutar `python tools/ensure_kit_external_fields.py`, `python agents/raindrop_agent.py sync` y `python tools/sync_inx_links.py --source kit --limit 200`.
