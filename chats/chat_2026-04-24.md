# Agent Chat — 2026-04-24

_Fuente de verdad compartida del día. Todos los agentes leen este hilo completo antes de responder. Append-only._

## Participantes

- **David** — director (rol fijo). Puede entrar desde cualquier interfaz en cualquier momento.
- **Copilot**, **Claude**, **Codex** — agentes intercambiables. Cualquiera puede asumir cualquier función cuando se le solicita.

## Funciones asignables (cualquier agente, a petición)

- orquestación, síntesis, integración en VS Code, visión de conjunto
- análisis profundo, razonamiento lógico, revisión crítica, teoría
- generación de código, refactoring, tests, implementación técnica

## Formato de mensajes

```
**David [@Destinatario]:** mensaje
**Agente:** mensaje | propuesta | voto | evaluación
```

## Modos de decisión

| Símbolo            | Modo        | Cuándo usarlo                                |
| ------------------ | ----------- | -------------------------------------------- |
| 🗳️ VOTO #N        | Votación    | Decisión binaria rápida, mayoría simple      |
| 🔍 EVALUACIÓN #N   | Evaluación  | Comparar alternativas desde cada especialidad |
| 🎯 ESPECIALIDAD    | Delegación  | Dominio inequívoco — solo David lo asigna    |
| 💡 CREATIVIDAD #N  | Brainstorm  | Sin restricciones, enfoque fresco            |

## Marcadores canónicos

- `MEMORIA:` hecho o acuerdo duradero
- `BLOQUEO:` impedimento concreto
- `SIGUIENTE:` siguiente acción u owner
- `✅ CERRADO #N:` decisión finalizada

## Estado actual

- Decisiones abiertas: ninguna
- Última acción: inicio de sesión 2026-04-24

---

**Codex:** Continué desde el estado local y cerré la desalineación detectada: `playbooks/` queda documentado en `memory/INDEX.md` y `memory/STRUCTURE.md`, el TREE fue regenerado, y corregí restos documentales de `github-rep-agent` y de Inoreader para reflejar el criterio vigente `kit-import` sin `starred` por defecto. Devlog actualizado en `[DOCS] Alinear playbooks e Inoreader en memoria`.

**Codex:** Seguí con la limpieza del estado local: `.claude/settings.json` queda ignorado y `tools/snapshot_structure.py` ya excluye configuración local, logs y estados locales de Inoreader del TREE. Validado con `py_compile` y `snapshot_structure.py --check`; devlog actualizado en `[TOOLING] Filtrar estado local en snapshot_structure`.

**Codex:** Añadí `playbooks/README.md` como índice mínimo de la nueva carpeta y regeneré `memory/STRUCTURE.md` para que el TREE lo recoja. Devlog actualizado en `[DOCS] Indice local de playbooks`.

**Codex:** Rematé la alineación de Inoreader: `docs/inoreader-agent.md` y el caso 15 ya presentan `kit-import` como flujo canónico; `starred` queda solo como advertencia o import manual explícito. Devlog actualizado en `[DOCS] Completar alineacion docs Inoreader kit-import`.

**Codex:** Ajusté también la ayuda CLI de `tools/import_inoreader_articles.py`: ahora declara `INOREADER_FOLDER_KIT` como default operativo y deja `starred` solo como etiqueta de trazabilidad si David pasa explícitamente una URL manual starred. Validado con `--help`, `py_compile` y `memory_check`; devlog actualizado en `[TOOLING] Aclarar ayuda CLI import_inoreader_articles`.

**Codex:** Commit creado: `7f0f9c3 Document playbooks and align Inoreader workflow`. Worktree limpio salvo ignorados locales (`.env`, `.venv/`, caches, logs y estados locales).
